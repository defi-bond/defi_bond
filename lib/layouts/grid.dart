/// Grid System
/// ------------------------------------------------------------------------------------------------

class SPDGrid {

  /// Defines the application's grid system.
  const SPDGrid._();

  /// The application's smallest unit of measurement.
  static const double x1 = 8.0;

  /// Return 2 times the application's smallest unit of measurement ([x1]).
  static const double x2 = x1 * 2.0;

  /// Return 3 times the application's smallest unit of measurement ([x1]).
  static const double x3 = x1 * 3.0;

  /// Return 4 times the application's smallest unit of measurement ([x1]).
  static const double x4 = x1 * 4.0;

  /// Return 5 times the application's smallest unit of measurement ([x1]).
  static double get x5 => x1 * 5.0;

  /// Return 6 times the application's smallest unit of measurement ([x1]).
  static double get x6 => x1 * 6.0;

  /// Return 7 times the application's smallest unit of measurement ([x1]).
  static double get x7 => x1 * 7.0;

  /// Return 8 times the application's smallest unit of measurement ([x1]).
  static double get x8 => x1 * 8.0;

  /// Return 9 times the application's smallest unit of measurement ([x1]).
  static double get x9 => x1 * 9.0;

  /// Return 10 times the application's smallest unit of measurement ([x1]).
  static double get x10 => x1 * 10.0;
}